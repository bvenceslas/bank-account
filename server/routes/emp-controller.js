const express = require('express');
const mongoose = require('mongoose');
const empRouter = express.Router();

const ObjectId = mongoose.ObjectId;

empRouter.route('/', (req, res) => {
    
})


module.exports = empRouter;